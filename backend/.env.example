# MongoDB connection used for persistent player and settings storage.
MONGO_URL=mongodb://127.0.0.1:27017
DB_NAME=fire_feast
FIRE_FEAST_ENV=development
# Comma-separated HTTPS origins required when FIRE_FEAST_ENV=production.
FIRE_FEAST_CORS_ORIGINS=
