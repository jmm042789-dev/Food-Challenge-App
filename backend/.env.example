# Required. Copy this file to .env for local development. The backend loads it
# without overriding variables already supplied by the OS. Never use it for
# production secrets.
MONGO_URL=mongodb+srv://jmm042789_db_user:YOUR_PASSWORD@firefeast-db.yefxsbg.mongodb.net/?appName=firefeast-db
DB_NAME=fire_feast
FIRE_FEAST_ENV=development
# Optional. Native clients do not require CORS. Browser origins must be
# comma-separated; production origins must use HTTPS.
FIRE_FEAST_CORS_ORIGINS=
