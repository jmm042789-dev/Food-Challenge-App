# Development may use a reachable LAN URL, for example http://192.168.1.10:8000.
# Production builds must provide a real HTTPS endpoint; no default is supplied.
EXPO_PUBLIC_BACKEND_URL=

# Optional Expo CLI optimization; this value is not exposed to application code.
EXPO_USE_FAST_RESOLVER=1
